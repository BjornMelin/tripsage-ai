# Core dependencies for TripSage PGVector Performance Benchmark Suite
# These dependencies are required for running the comprehensive benchmark framework

# Core Python packages already in project
numpy>=1.24.0
pydantic>=2.0.0
psutil>=5.9.0

# Statistical analysis for regression detection
scipy>=1.10.0

# CLI interface for CI/CD integration
click>=8.1.0

# Performance monitoring and profiling
memory-profiler>=0.60.0

# Database and async support (should already be in project)
asyncio-tools>=0.1.0

# Optional: Advanced visualization for reports (if needed)
plotly>=5.14.0  # For interactive performance charts
matplotlib>=3.6.0  # For static performance charts

# Optional: Notification support for CI/CD
requests>=2.28.0  # For webhook notifications
slack-sdk>=3.21.0  # For Slack notifications (optional)

# Testing dependencies (for running benchmark tests)
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Development and debugging
ipython>=8.0.0  # For interactive debugging
jupyter>=1.0.0  # For notebook-based analysis (optional)

# Note: The following are assumed to be already available from the main project:
# - supabase (for database connectivity)
# - tripsage_core (for core services)
# - Other TripSage-specific dependencies